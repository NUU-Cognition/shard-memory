---
id: {{uuid}}
tags:
  - "#mem/index"
  - "#managed/shard/mem"
---

# Memory Index

The always-loaded entry point to this Flint's memory. **Read this first.** It lists every **active** memory with a one-line description of what it covers and when to load it — open an individual memory note only when its line matches your task. Logged (historical) memories are intentionally not listed here.

Maintained by the Memory shard skills (`remember`, `revise`, `log`) and rebuilt by `sk-mem-index`. Keep it lean.

## Memories

*No memories yet. Capture one with `sk-mem-remember` (or the "Remember" skill) and it will be listed here.*

<!-- One line per active memory, e.g.:
- [[(Memory) 001 Flint CLI is not run on WSL]] — Whether/how the Flint CLI runs under WSL. Load when working on CLI setup or environment issues.
-->
